
// if you want the longest subbarray:

function longestSub() {
    for(end = 0; end < length; end++) {
        // expand window
        while feasible () == False: 
        // keep doing something while false (while expanding if condition are not met keep shrinking window)
        // shrink window
    }
}

function smallestSub() {
    for(end = 0; end < length; end++) {
        // expand window
    }
    while feasible () == True:
    // while the condition is true, keep shrinking window
}